# AI Context & Planning (AGENTS.md)

## 1. Project Overview

**Project Name:** MARK (Metacognitive Artificial Relational Knowledge) — v5.0.0
**Description:** A privacy-first, local-based autonomous AI OS companion designed to assist user productivity, automate tasks, and provide lifelike companionship. It uses a hybrid AI engine (Local LLM via LM Studio or Cloud API, plus a native Gemini Web RPC Engine) and features agentic planning with ReAct loop execution, **Autonomous Multi-Agent Sub-Agent Engine** (UI: **Sub-Agents**, branding: **Mission Control**) with concurrent isolated browser sessions, **Durable Agent Tasks** (UI: **Agent Workflows**) for persistent multi-step work, autonomous physical browser automation with multi-session support, persistent C# Win32 desktop automation daemon, a hybrid Full-Text & Vector Memory Management System (MMS) with Orama & Dexie, document RAG pipeline, OS-level Awareness Engine, dynamic 4D Relational Growth, a native Plugin System with Monaco Editor, Telegram Bot integration via Telegraf, Voice Activity Detection with Groq Whisper STT, Edge-TTS, and webcam vision capabilities.
**Environment:** Tauri v2 desktop application optimized for Windows (Windows 10/11) with Microsoft Edge WebView2 & standalone Node.js backend.
**Author:** Mazees | **Homepage:** https://github.com/Mazees/mark-agent/

## 2. Technology Stack & Core Dependencies

- **Framework:** Tauri v2 (Rust Host), React 19, Vite 7
- **UI/Styling:** Tailwind CSS 4 (via `@tailwindcss/vite`), DaisyUI 5 (theme: `forest`), Poppins + Inter fonts, React Markdown, React Syntax Highlighter (Prism, oneDark), Monaco Editor (`@monaco-editor/react`), Driver.js (guided tours), Lucide React, React Icons
- **AI Backend:** Gemini Web RPC (Native Bridge) / Groq API / LM Studio (Local, `localhost:1234`) / Cerebras / Custom OpenAI-compatible Endpoint
- **Embeddings/Memory:** `@huggingface/transformers` (Transformers.js) for fully local embeddings via WASM (`Xenova/paraphrase-multilingual-MiniLM-L12-v2`, 384 dimensions)
- **Local Database & Vector Search:** `dexie` (IndexedDB wrapper, v14 schema with 8 stores) and `@orama/orama` for Hybrid Full-Text & Vector search
- **Desktop Host (Rust):** `src-tauri/` handling window lifecycle, WebView2 container, system tray, global shortcut `Ctrl+Alt+M`, single-instance lock, GDI screen capture, and Node.js process supervisor.
- **Backend & Native OS Tools (Node.js):** `src/backend/engine.js` stdio JSON RPC daemon powering file CRUD, PowerShell execution, multi-session browser automation, PC automation daemon, plugins, skills, and Telegram bot.
- **Voice/Audio:** Web Speech API (Edge Native Speech-to-Text with Indonesian `id-ID` locale, RAM 0 MB), Edge-TTS (`msedge-tts`, voice: `id-ID-ArdiNeural`), Web Audio API (Real-time volume visualizer & FFT intensity extraction)
- **Media/Integrations:** `youtube-transcript-plus`, `ytmusic-api` (YouTube Music with ad-blaster), `yt-search`, `youtube-dl-exec` + `ffmpeg-static`
- **Communication:** `telegraf` (Telegram Bot Framework)
- **Packaging:** `tauri build` (NSIS installer, `identifier: com.mark.agent`)

## 3. Project Architecture & File Structure

### `src-tauri/` — Tauri v2 Host Process (Rust)

| File                     | Purpose                        | Key Details                                                                                                              |
| ------------------------ | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| `src/main.rs`            | Entry point                    | Calls `mark_lib::run()`                                                                                                  |
| `src/lib.rs`             | Core App Builder & Tray        | Window creation, System Tray menu, global shortcut `Ctrl+Alt+M`, single-instance lock, window events, Node process spawn |
| `src/cmd_node_bridge.rs` | **Node.js Process Supervisor** | Spawns and manages `src/backend/engine.js`, routes JSON RPC stdio commands, auto-forwards background events to renderer  |
| `src/cmd_window.rs`      | Window & System Helpers        | Minimize, maximize, close, notification, open external URL, get documents path                                           |
| `src/cmd_screenshot.rs`  | Native GDI Screen Capture      | 720p desktop screen capture returning base64 JPEG                                                                        |

### `backend/` — Node.js Backend Engine

| File                           | Purpose                                     | Key Details                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `engine.js`                    | **Central Stdio JSON RPC Router**           | Connects all native OS tools, Telegram bot, Skills, Plugins, and AI HTTP bridge to Tauri via standard input/output streams                                                                                                                                                                                                                                                                                                                                                           |
| `ai-bridge.js`                 | Centralized AI HTTP client                  | `fetchAI()` with multi-provider routing (Groq/Cerebras/Custom/LM Studio), 3-tier JSON format fallback (`json_schema` → `json_object` → unrestricted), rate limiting (`CLOUD_DELAY_MS=3000`), 429/503 exponential backoff with auto model swap to `openai/gpt-oss-20b`, vision payload sanitization (strips old images), DeepSeek `<think>` tag extraction, `cleanAndParse()` with `jsonrepair`, 5-minute timeout for local LLM                                                       |
| `browser-agent.js`             | **Multi-Session Browser Automation Engine** | Manages `Map<sessionId, SessionState>()` supporting isolated concurrent Chromium windows for Lead Agent and parallel Sub-Agents. DOM parser tagging max 80 elements with `data-mark-id`, glassmorphism blocking overlay, animated SVG cursor, React 18+ input binding, `browser-ask-user` unblock mode with title communication (`MARK_UNBLOCK_DONE:`), popup blocking via `setWindowOpenHandler`, 60s load timeout, per-session base64 preview thumbnails broadcasting to renderer. |
| `pc-agent.js`                  | High-Speed Desktop Automation               | Zero-Vision cost Windows PC Automation Engine via persistent PowerShell C# Daemon (`pc-daemon.ps1`). Executes mouse clicks, keyboard typing (SendInput Unicode), shortcut keys, scrolling, app opening, and window management via Win32 API. Exposes 9 IPC tools: `os-read`, `os-click`, `os-type`, `os-key`, `os-scroll`, `os-open`, `os-list-windows`, `os-focus-window`, `os-ask`. Features floating lock overlay and global `Ctrl+Shift+S` emergency stop.                       |
| `node-tools.js`                | OS Tool Registry                            | Complete registry for native OS tools: file CRUD (`read-file`, `write-file`, `replace-lines`, `delete-file`, `list-dir`, `grep-search`), `run-powershell` with dangerous keyword blacklist, multi-session browser-\* dispatchers with `sessionId` propagation, and OS tools. Delimiter: double-pipe `\|\|`.                                                                                                                                                                          |
| `awareness/window-tracker.js`  | OS activity monitoring                      | `active-win` every 60s, `powerMonitor.getSystemIdleTime()` idle detection (>180s), 30-entry ring buffer, timestamps in `id-ID` locale                                                                                                                                                                                                                                                                                                                                                |
| `plugins/plugin-loader.js`     | Plugin lifecycle manager                    | Scans `~/Documents/Mark Plugins/`, dynamic ESM import with cache-busting (`?t=Date.now()`), `plugin.json` + `index.js` structure, auto `npm install` on creation, code extraction for Monaco Editor UI, IPC endpoints for CRUD/toggle/reload                                                                                                                                                                                                                                         |
| `telegram/telegram-service.js` | Telegram Bot Engine                         | `Telegraf` bot framework, polling mode, `/start` and `/register` admin approval flow, text and media processing, screenshot triggers, music command bridge, and direct UI notification integration.                                                                                                                                                                                                                                                                                  |
| `services/gemini-web.js`       | Gemini Web RPC Engine                       | Connects directly to Google's Gemini Web RPC backend. Supports models like `gemini-3.6-flash`, `gemini-3.5-flash-thinking`, `gemini-flash-lite`, and `gemini-auto`. Integrated directly into `ai-bridge.js`.                                                                                                                                                                                                                                                                         |
| `skills/skill-manager.js`      | Skill Manager                               | Manages custom skills, VSCode-like file tree structure, ZIP package extraction, and dynamic skill watcher                                                                                                                                                                                                                                                                                                                                                                            |

### `frontend/api/` — Core Logic & Multi-Agent Layer

| File                           | Purpose                             | Key Details                                                                                                                                                                                                                                                                                                                         |
| ------------------------------ | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `db.js`                        | Dexie database (v14 schema)         | 8 stores: `memory`, `sessions`, `config`, `chatArchive`, `documents`, `relationships`, `subagents` (id, name, role, goal, status, turnCount, createdAt, updatedAt), `subagentMessages` (id, subagentId, sender, role, content, timestamp).                                                                                          |
| `subagent/subagentStore.js`    | Sub-Agent State & Persistence Store | Dexie-backed reactive CRUD for Sub-Agents & message streams. Functions: `createSubagent`, `getSubagent`, `getAllSubagents`, `updateSubagent`, `deleteSubagent`, `addMessage`, `getMessages`. Subscriptions for live UI reactiveness.                                                                                                |
| `subagent/subagentExecutor.js` | Autonomous Sub-Agent ReAct Runner   | Runs isolated ReAct tool loop for spawned sub-agents without artificial turn boundaries. Dispatches native tools with `{ sessionId: subagentId }` for isolated browser instances. Intercepts `read-tools` for dynamic group tool expansion. Automatically cleans up browser session on complete or abort (`killSubagentExecution`). |
| `subagent/subagentPrompt.js`   | Sub-Agent System Prompt Generator   | Pure utilitarian, goal-oriented system prompt. Structure: Core built-in tools + Additional Tool Groups (`advanced_browser`, `pc_automation`, `youtube_music`, etc.) with `read-tools` dynamic guide lookup. Enforces strict JSON `{ thought, action, answer }` responses and resilience against user encouragement whispers.        |
| `vectorMemory.js`              | Embedding engine                    | Model: `Xenova/paraphrase-multilingual-MiniLM-L12-v2` (384d). `generateVector()`, `cosineSimilarity()`, `getRelevantMemory()`, `searchExtendedMemory()` (threshold **0.3**), `getUnifiedContext()`.                                                                                                                                 |
| `oramaStore.js`                | Hybrid vector+fulltext search       | `archiveIndex` + `documentIndex` (both 384d vectors). `hydrateFromDexie()`. `searchArchives()` (threshold **0.25**). `searchDocuments()` (threshold **0.25**). `findSimilarMemoryClusters()`.                                                                                                                                       |
| `ragPipeline.js`               | Document ingestion                  | `ingestDocument()`: max 50MB, `.pdf`/`.docx` via IPC `parseDocument`, `.txt`/`.md` raw read. `splitTextIntoChunks()` (500 chars, 50 overlap). Dual-save to Dexie + Orama.                                                                                                                                                           |
| `scraping.js`                  | Web scraping engine                 | `scrapeGoogle()` + `deepSearch()`: DOM cleanup, extracts article text, 1500 char cap.                                                                                                                                                                                                                                               |

### `frontend/api/ai/` — AI Brain & Orchestration

| File                | Purpose                     | Key Details                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------- | --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `core.js`           | AI IPC wrapper              | `fetchAI()` with `AbortSignal` support via IPC. `cleanAndParse()`: multi-stage JSON parser (strip markdown fences → extract `{}` substring → clean control chars/trailing commas → `jsonrepair` fallback)                                                                                                                                                                                                                                       |
| `planning.js`       | Agentic planner & router    | **`CATEGORY_TEXTS`** (7 categories) with vector matching (threshold **0.35**). `getNextAction()`: dynamic system prompt assembling persona, tools, plugins, memory, and **Multi-Agent Orchestration rules**. Proactively instructs Lead Agent to spawn parallel sub-agents (`spawn_subagent`, `wait_subagents`, `send_message`, `list_subagents`, `kill_subagent`) for multi-topic research and complex workloads. Handles batch actions array. |
| `persona.js`        | Character & personality     | `getPersonaPrompt()`: Windows Electron PC identity, Jarvis vibe, sarcasm scaling (`<0.65` subtle, `>=0.65` roasting), adaptive tone (lu/gue vs Saya/Anda), TTS formatting, 10 mood types. `getTraitContext()`: injects 4D trait values into prompt.                                                                                                                                                                                             |
| `relationship.js`   | Relational growth evaluator | `evaluateTraitDrift()`: `TRAIT_KEYS` = warmth/sarcasm_level/trust/energy. `MAX_DRIFT=0.01` per step. `FLOOR` = warmth:0.15, trust:0.15. Clamps 0-1.                                                                                                                                                                                                                                                                                             |
| `awareness.js`      | Autonomous awareness AI     | `getAwarenessResponse()`: evaluates OS activity buffer, memory refs, current time, music state. Prompt includes capabilities list, ethics rules. JSON schema output: `should_act`, `message`, `autonomous_prompt`, `mood`.                                                                                                                                                                                                                      |
| `chatSummarizer.js` | Chat archival               | `summarizeAndArchive()`: distills recent messages into 2-3 sentence summary via LLM, generates embedding vector, dual-saves to Dexie `chatArchive` + Orama `archiveIndex`.                                                                                                                                                                                                                                                                      |
| `memoryGroomer.js`  | Memory Groomer Engine       | `buildGroomerPrompt(clusters)`, `parseGroomerResponse(raw)`, `runBatchConsolidation()`. Merges chronological profile & preference memories without losing history.                                                                                                                                                                                                                                                                              |
| `tools.js`          | YouTube & Music AI          | `getYoutubeSummary()`: fetches transcript via IPC, chunks at 4000 chars, 12s delay. `getBestMusicMatch()`: AI ranker selects best song from search results.                                                                                                                                                                                                                                                                                     |
| `utils.js`          | Shared utilities            | `getCurrentTimeInfo()` (Indonesian locale), `playVoice()` (TTS via IPC, sets `window.isMarkSpeaking`).                                                                                                                                                                                                                                                                                                                                          |

### `src/renderer/src/components/subagent/` & `pages/`

| Component / Page                           | Purpose                             | Key Details                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------ | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pages/Subagents.jsx`                      | **Mission Control Dashboard**       | Comprehensive dashboard for tracking all background and active Sub-Agents: status indicators (`running`, `completed`, `failed`, `killed`), turn counters, creation timestamps, manual execution controls, and deep-dive Intercom panel modal.                                                                                                                                 |
| `components/subagent/SubagentIntercom.jsx` | **Sub-Agent Live Intercom & HUD**   | Real-time communication bridge between Lead Agent (Mark), User (Mada), and Sub-Agents. Groups consecutive turns into high-contrast single bubbles (`bg-base-200/95`) with **HUD Double Dropdown**: `Pemikiran Sub-Agent` (`Brain`) and `Langkah Eksekusi` (`Terminal`, formatted as `1. tool : query` with expandable output). Full Markdown & CodeBlock support for answers. |
| `components/core/BrowserPreviewWidget.jsx` | **Multi-Card Holo Browser Preview** | Cascading floating draggable HoloCards rendering real-time thumbnails for all active concurrent browser sessions (`default` + `sub_*`). Provides independent `Buka Jendela` and `Tutup Sesi` controls per session.                                                                                                                                                            |

## 4. Key Implementation Invariants & Gotchas

### Multi-Agent Sub-Agent Architecture

- **No Turn Limit (`maxTurns`)**: Sub-agents execute autonomously until their goal is fulfilled (action: null, answer provided) or until explicitly aborted/killed.
- **Session-Isolated Browser Windows**: Every sub-agent receives an isolated `BrowserWindow` instance mapped to its unique `subagentId`. All browser actions (`browser-navigate`, `browser-read`, `browser-click`, `browser-close`) execute on the agent's dedicated session without cross-agent contamination.
- **Proactive Orchestration**: Lead Agent (Mark) is instructed to proactively split multi-topic research into parallel batch spawns (`spawn_subagent` in batch array) and gather aggregated insights via `wait_subagents`.

### Critical Constants & Thresholds

| Constant                   | Value                     | Location                 | Purpose                             |
| -------------------------- | ------------------------- | ------------------------ | ----------------------------------- |
| Vector Similarity (Memory) | **0.3**                   | `vectorMemory.js`        | Filter irrelevant extended memories |
| Vector Similarity (Orama)  | **0.25**                  | `oramaStore.js`          | Archive & document search threshold |
| Category Router            | **0.35**                  | `planning.js`            | Intent matching for tool exposure   |
| Trait Drift                | **0.01** max              | `relationship.js`        | Max trait change per evaluation     |
| Trait Floor                | **0.15** (warmth, trust)  | `relationship.js`        | Minimum allowed trait values        |
| Awareness Cooldown         | **9 minutes**             | `useAwareness.js`        | Prevents check-in spam              |
| Awareness Interval         | **10 minutes**            | `useAwareness.js`        | Check-in loop period                |
| Relational Eval Interval   | **15 clean messages**     | `useRelationalGrowth.js` | Trait evaluation trigger            |
| Cloud Rate Limit Delay     | **3000ms**                | `ai-bridge.js`           | Min gap between cloud API calls     |
| Activity Buffer Size       | **30 entries**            | `window-tracker.js`      | Ring buffer cap                     |
| DOM Parser Elements        | **80 max**                | `browser-agent.js`       | Max interactive elements tagged     |
| RAG Chunk Size             | **500 chars, 50 overlap** | `ragPipeline.js`         | Document chunking params            |
| YT Summary Chunk           | **4000 chars**            | `tools.js`               | Transcript chunk boundary           |
| VAD Speech Threshold       | **RMS > 0.015**           | `useVAD.js`              | Voice detection sensitivity         |
| VAD Silence Timeout        | **2000ms**                | `useVAD.js`              | Auto-cut after silence              |

## 5. Development Guidelines for AI Agents

- **Read Before Modify:** Always read the corresponding `api/`, `subagent/`, or `hooks/` file entirely before modifying state or logic.
- **Maintain Privacy-First Paradigm:** Avoid adding third-party tracking, analytics, or mandatory cloud dependencies. Everything must be able to fall back to 100% offline local state.
- **Electron Process Boundary:** Never mix Node.js native APIs directly in the `renderer/` folder. All OS interactions must pass through `preload/index.js` → IPC → `main/`.
- **UI Design System:** The UI uses Tailwind CSS 4 + DaisyUI 5 (`forest` theme) with custom holographic/glassmorphic design tokens in `main.css`.
- **Strict Emoji Rule:** Dilarang keras menggunakan emoji apapun di dalam respon output, dialog, maupun UI.
